specVersion: "v1beta"
serviceId: "todo-ls"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "todo-ls-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
